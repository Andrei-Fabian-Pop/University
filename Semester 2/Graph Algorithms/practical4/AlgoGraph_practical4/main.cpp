#include "UI.h"

// 4. Write a program that, given a graph with costs, does the following:
//
// verify if the corresponding graph is a DAG and performs a topological sorting of the activities
// using the algorithm based on predecessor counters; if it is a DAG, finds the highest cost path between two given vertices, in O(m+n).

int main() {
    UI::start();

    return 0;
}
